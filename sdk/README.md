# SDK

The Python package under `sdk/python/wdl_sdk` provides a game-independent reader and search API for the research database. It uses only the Python standard library.

Future process-memory, signature, and live-game interfaces will be added only after their ownership, error behavior, safety model, tests, and build contracts are documented.
