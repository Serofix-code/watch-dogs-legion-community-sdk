# Research database

Each JSON file under `records/` contains an array of evidence-bearing records. Records use stable namespaced IDs and explicitly list confidence, status, observed builds, evidence, related symbols, and unresolved questions.

The canonical schema is `schemas/research-record.schema.json`. Generated documentation must not be edited manually.
