# Build compatibility

Preferred identity fields are platform, distribution, module filename, module SHA-256, observation date, and notes.

Current records reference an observed Steam PC DX11 session using `DuniaDemo_clang_64_dx11.dll`. The exact hash is pending, so other builds are unsupported until tested. Submit module hashes only—never game modules.
